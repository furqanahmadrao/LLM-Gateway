# LLM Gateway Environment Variables

# Server
PORT=3000
LOG_LEVEL=info

# Base URL Configuration (optional)
# Set this when deploying behind a reverse proxy or custom domain
# Example: https://api.example.com/gateway
# BASE_URL=https://api.example.com

# Host Header Validation Mode (strict or permissive)
# strict: reject requests with mismatched Host header
# permissive: log warning but allow requests (default)
HOST_VALIDATION_MODE=permissive

# Database
DATABASE_URL=postgresql://gateway:password@localhost:5432/gateway

# Redis
REDIS_URL=redis://localhost:6379

# Security
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# Rate Limiting
RATE_LIMIT_RPM=60

# Cache
MODEL_CACHE_TTL=3600

# Demo Mode
# Set to 'true' to enable mock data in the dashboard (for demonstration purposes)
# Default: false (production mode - shows real data only)
DEMO_MODE=false
